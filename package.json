{
  "name": "sorofund-platform",
  "version": "1.0.0",
  "description": "SoroFund - Decentralized Milestone-based Crowdfunding on Stellar",
  "scripts": {
    "dev:frontend": "cd frontend && npm run dev",
    "dev:backend": "cd backend && npm run dev",
    "dev": "concurrently \"npm run dev:frontend\" \"npm run dev:backend\"",
    "install:all": "npm install && cd frontend && npm install && cd ../backend && npm install",
    "build:contracts": "cd contracts/sorofund && stellar contract build",
    "test:contracts": "cd contracts/sorofund && cargo test"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
